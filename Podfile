# Uncomment the next line to define a global platform for your project
  platform :ios, '9.0'

target 'Espo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Espo
   pod 'Firebase'
   pod 'Firebase/Auth'
   pod 'Firebase/Firestore'

  target 'EspoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'EspoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
